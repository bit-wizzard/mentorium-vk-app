import styled from 'styled-components'

export const TitleStyle = styled.div `
    display: grid;
    grid-template-rows: auto 1fr;
    align-items: center;
    grid-gap: 1rem;
    .chevron {
        opacity: 0;
    }
`